---
title: Dell Wyse 5070 折腾记
date: 2026-2-15
tags: [嵌入式, 垃圾佬, 软路由, NAS, 待更新]
categories: 捡垃圾
---

<p/>
<!--more-->

{% note info %}

### 这篇文章还在更新中

{% endnote %}

# 一切的起因

得益于尼禄优秀的校园网机制，每次联网都要跳转网页认证，ip还会不定时改变，导致远程连接宿舍电脑变得极其麻烦。为了省去认证步骤以及每次出门前开电脑记ip的麻烦，我就打算弄台openwrt软路由。一如既往，我的脑子又开始意淫：既然有了小主机，为什么不跑docker，为什么不做nas，为什么不玩IoT……

# 硬件选择

我的这台主机是J5005高配版，胖版的机箱空间更大，但是没有瘦版的emmc。
Wyse 5070只有一个千兆网口，为了跑软路由，我把M.2 WiFi口转成了RTL8125B 2.5G网口（M.2 A+E key）。
另外还有一个M.2插槽，我本想那它拓展出几个sata盘位，等转接卡到了我才发现这个M.2接口**不支持NVME协议**，只能纯物理方法转接为单个sata口。我找了半天才找到卖**ngff转sata转接卡**的（自带供电，不错），但是正值春节快递停运，要等半个月才能拿到……
内存方面，主板带有两个ddr4插槽，本来官网上写的这颗cpu最高只支持8G内存，但实测更新完bios（1.40）后可以识别16G+8G内存。
硬盘用的是之前买的64G创见拆机固态，本来以为是杂牌，后来才发现在服务器端见的比较多，用料挺不错的，甚至还是MLC颗粒。
主板上还有一个PCIe x4插槽（只有胖版主机焊了），这个我打算后续收一块Google的Edge Coral tpu用来做图像识别（同人图分类）。

# 安装系统

我选用的宿主系统是debian，openwrt挂在kvm上

第一次登录时出了点意外，卡在tty的login，怎么输入都没反应。后来重插了一下键盘才解决（

# 配置环境

不得不说，debian和所有我之前用过的Linux发行版体验都很不一样，开荒阶段极其痛苦。

首先就是喜闻乐见的普通用户不在sudo用户组无法使用sudo命令。
先安装sudo（如果是最小化安装）：

```bash
apt install sudo
```

将目标用户（"targetuser"）添加到sudo组：

```bash
sudo usermod -aG sudo targetuser
```

验证：

```bash
id targetuser
```

应出现`27(sudo)`，完成

其次，由于Debian很多底层工具是用python写的，为防止系统自带python环境被破坏，设置了保护机制禁止pip安装包，只能暴力突破或使用虚拟环境。

再安装一些常用工具：

```bash
sudo apt install net-tools # 以使用ifconfig命令
sudo aot install memtester # 内存测试工具
                           # ……
```

## RTL8125B 网卡驱动安装

待更新

## 安装 Cockpit

安装核心组件：

```bash
apt update && apt install -y cockpit cockpit-machines cockpit-podman qemu-kvm libvirt-daemon-system libvirt-clients bridge-utils curl git build-essential cmake
```

- `cockpit-machines`：管理 KVM 虚拟机
- `cockpit-podman`：管理容器（备用）

启用Cockpit：

```bash
systemctl enable --now cockpit.socket
```

## 安装 Docker

```bash
curl -fsSL https://get.docker.com | bash
```

## 安装V2rayA

由于要挂tg bot和拉取docker镜像，科学上网环境必须配置。

启动容器：

```bash
docker run -d \
  --restart=always \
  --privileged \
  --network=host \
  --name v2raya \
  -e V2RAYA_LOG_FILE=/tmp/v2raya.log \
  -v /lib/modules:/lib/modules:ro \
  -v /etc/resolv.conf:/etc/resolv.conf \
  -v /etc/v2raya:/etc/v2raya \
  mzz2017/v2raya
```

- `--network=host`：直接接管主机网络，省去端口映射的麻烦

配置订阅：

- 访问`http://<主机IP>:2017`
- 创建管理员账号
- 导入订阅链接
- **设置** -> 勾选**开启端口分享**，保存 -> 连接一个节点 -> 左上角**启动**

{% note warning %}

### 注意：

如果要使用软路由，**不要**开启“透明代理”，不然所有流量都会走代理
{% endnote %}

# 安装 OpenWRT

待更新

# 安装 AstrBot + NapCat

待更新

# 一些碎碎念

- 内存涨价太疯了，导购我阐述你的梦。
- 其实升级bios时翻车了，差点变砖，还好Gemini大人提醒我可以用Dell官方的bios修复工具。
- 我最初的想法是在本地部署qwen2.5 3b的，结果发现0.2tok/s的速度都跑不到，无奈只能用API了。
